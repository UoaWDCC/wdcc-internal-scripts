export const parseCsv = (filePath: string): unknown[] => {
  console.log(filePath);
  return [];
};

export const writeCsv = (data: unknown[], filePath: string): void => {
  console.log(data, filePath);
  return;
};
